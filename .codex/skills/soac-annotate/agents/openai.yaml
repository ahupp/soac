interface:
  display_name: "SOAC Annotate"
  short_description: "Profile a Python snippet and annotate post-opt SOAC views"
  default_prompt: "Use this skill when you need to run a small Python snippet under SOAC with an example workload, collect post-opt-v3 BlockPy, specialized CLIF, or VCode, annotate the requested view inline, and answer optimization questions about the resulting blocks."
