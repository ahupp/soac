# soac_py/src/soac/__init__.py

## File Responsibilities

Package initializer for `soac`. It eagerly imports the native `_soac_ext` extension so package consumers can reach the extension module through `soac._soac_ext`.

## Datatypes

- `__all__`: exports `_soac_ext`.

## Functions

- No functions are defined.

## Context Read

- `soac-pyo3/src/lib.rs`

